/**
 * B2C Workspace Settings Page (SKELETON)
 */

function WorkspaceSettings() {
    return (
        <div style={{ padding: '2rem' }}>
            <h1>Workspace Settings</h1>
            <div style={{
                padding: '2rem',
                border: '2px dashed #ccc',
                borderRadius: '8px',
                textAlign: 'center'
            }}>
                <p><strong>🚧 Skeleton Component</strong></p>
                <p>Implement workspace settings here (name, subscription, members, etc.)</p>
            </div>
        </div>
    );
}

export default WorkspaceSettings;
